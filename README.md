# dzz-expenses

A personal expense tracker. Log expenses by category, see spending charts on the dashboard, check a monthly summary, and export to Excel. Each user's data is private to their account.

Built with TanStack Start, React, TypeScript, Tailwind CSS, and Supabase (auth + database).

## Running locally

```sh
npm i
npm run dev
```
